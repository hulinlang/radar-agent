# =============================================================================
# radar-agent · 最终项目仓库的取舍原则
# =============================================================================
#
# 只保留 7 类：
#   ① 基模的配置与获取说明（权重太大不进仓库，见 README「模型」节）
#   ② 数据集             → data_processed/sft_v1/、data_processed/eval/
#   ③ 配置文件           → configs/
#   ④ 训练文件           → scripts/p5_train_lora.py、src/dataset/
#   ⑤ 评测文件           → scripts/p5_eval_*.py、p6_eval_*.py、p7_eval_*.py、src/eval/
#   ⑥ RAG 知识库构建源码  → src/retrieval/（+ 切分产物 corpus/*.jsonl）
#   ⑦ Agent 框架(ReAct)  → src/agent/、src/tools/
#   外加：src/serve/（演示界面）、docs/、reports 的结论报告
#
# 过程性的探测、验证、日志一律**本地保留、不上传**：
#   它们的价值是"当时怎么得出结论"，而结论已写进 reports/*.md 与 docs/。
#   原始证据留在本地 logs/probe/（1061 个文件、6.1 MB）随时可查，不占仓库。
#
# ⚠️️ 两条语法红线（都踩过，代价是规则静默失效）：
#   A. 行尾注释**无效** —— gitignore 只认行首的 #。
#      写 `!reports/*.md   # 报告` 会让整行变成一个含 "# 报告" 字面的模式，
#      结果既不匹配任何文件、也不报错 → 规则静默失效。本文件所有注释都单独占行。
#   B. 父目录用 `dir/` 整体排除后，子项无法用 `!` 恢复；
#      要豁免子项，父目录必须写成 `dir/*`。
# =============================================================================


# -----------------------------------------------------------------------------
# ① 模型权重与训练产物（体积过大 / 可重训）
# -----------------------------------------------------------------------------
models/
outputs/
merged/
*.safetensors
*.gguf
*.bin
*.pt
*.pth
*.ckpt
*.onnx
adapter_model.*
optimizer.pt


# -----------------------------------------------------------------------------
# 过程产物：中间验证、日志、安装记录（本地保留）
# -----------------------------------------------------------------------------
logs/


# -----------------------------------------------------------------------------
# 数据：一律不上传（用户 2026-09-21 决定）
#   data_processed/sft_v1/   训练集与冻结评测集
#   data_processed/eval/     文献域检索评测集
#   data_processed/corpus/   知识库卡片（教材与论文的原文切片，另有版权顾虑）
#   data_processed/index/    检索索引（dense.npy 37 MB）
#   data_authored/           人工编写的题型样例与执行单元（含教材引用）
# 全部可由 scripts/p4_*.py、p5b_build_tool_sft.py 从原始语料重建。
# 模型权重改由 Release 附件分发（见 README「模型与数据：如何获取」节）。
# -----------------------------------------------------------------------------
data_processed/
data_authored/


# -----------------------------------------------------------------------------
# 评测原始逐条数据（体积大且只是过程）
# 结论已写进 reports 的 md；需要原始数据时用 scripts/p*_eval_*.py 重跑即可
# -----------------------------------------------------------------------------
reports/*
!reports/*.md
!reports/*.html


# -----------------------------------------------------------------------------
# 原始知识源：版权受限 / 外部文档站资源
# -----------------------------------------------------------------------------
data_raw/*.pdf
data_raw/*.epub
data_raw/*.docx
data_raw/**/*.pdf
data_raw/*.js
data_raw/*.css
data_raw/*.png
data_raw/*.html


# -----------------------------------------------------------------------------
# 第二套语料：以 submodule 引入，不复制内容
# 来源 https://github.com/matreshka15/mmWave_Insight（公开仓库，main HEAD = 8978a7ab）
# 配置见 .gitmodules；取全量用 git clone --recurse-submodules <本仓库>
# 理由：该目录自带 .git，身份由 git HEAD 唯一确定（见目录内 _intake_manifest.json），
#      复制 62 个文件（6.9 MB）进来等于重复造指纹。
# -----------------------------------------------------------------------------


# -----------------------------------------------------------------------------
# Python
# -----------------------------------------------------------------------------
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/
.pytest_cache/
.mypy_cache/
.ipynb_checkpoints/


# -----------------------------------------------------------------------------
# 编辑器 / 系统
# -----------------------------------------------------------------------------
.vscode/
.idea/
.DS_Store
Thumbs.db
desktop.ini


# -----------------------------------------------------------------------------
# 临时探测文件
# -----------------------------------------------------------------------------
_*.txt
_*.ps1


# -----------------------------------------------------------------------------
# 打包产物
# 模型已随 Release v1.0 附件分发，本地打的 zip 不入库（否则 61 MB 会被误收）
# -----------------------------------------------------------------------------
*.zip
