# P3 训练/评测去污染检测（R6）

> 由 `scripts/p3_decontaminate.py` 自动生成 —— 评测集**冻结前**必跑，重叠率必须写进报告。

- 训练集 1121 条 / 评测集 200 条
- 文本检测：字符 8-gram，Jaccard ≥ 0.60 视为疑似污染
- 图像检测：`image.sha256` 交集（必须为空）
- 参数检测：同公式 + 同参数组在两集同时出现（必须为空）

## 一、图像 sha256 重叠

✅ 无重叠（训练 70 张 / 评测 17 张，互不重复）

## 二、calc 同公式同参数重复

✅ 无同参数重复（同公式不同参数，符合 §二）

## 三、题面 n-gram 重叠（≥ 0.60）

✅ 无高重叠题面（阈值 0.60；**同概念换问法**是允许的，本项只看题面文本）

---

**结论：✅ 干净，可以冻结**