# P3 执行单元 U01 · 教材第2章(a) 信号表示/模糊函数/分辨率（14 条）

> 数据源：`logs/probe/_u01_out/p3_ch02a_v0.jsonl`　|　共 **14** 条
> 
> 语料：《机载雷达系统与信息处理》（电子工业出版社 2021.8，ISBN 978-7-121-41746-7）
> ＋ GitHub `matreshka15/mmWave_Insight`（Markdown 文档站，21 篇）
> 
> ⚠️ 本文件由 `scripts/p2_render_samples.py` **从编译产物生成**，不是手写；
> 因此下面展示的答案与将来模型看到的是**同一份字节**。
> ⚠️ 页码口径：一律 **PDF 页序（1-based）**，与书内印刷页码不同。

## 一、题型覆盖率（spec 里登记的**全部**题型）

| 题型 | 中文 | 条数 | 判分方式 |
|---|---|---|---|
| `calc` | 数值计算 | 5 ✅ | numeric |
| `choice` | 选择 | 2 ✅ | choice |
| `clarify` | 澄清 | 0 ❌ **缺** | — |
| `compare` | 图内对比 | 0 ❌ **缺** | — |
| `concept` | 概念简答 | 5 ✅ | keyword |
| `contrast` | 双体制对比 | 0 ❌ **缺** | — |
| `figure_qa` | 看图理解 | 0 ❌ **缺** | — |
| `readout` | 读图取值 | 0 ❌ **缺** | — |
| `regime_trap` | 体制陷阱 | 0 ❌ **缺** | — |
| `term` | 术语 | 1 ✅ | keyword |
| `trend` | 读图趋势 | 0 ❌ **缺** | — |
| `unanswerable` | 不可答（拒答） | 1 ✅ | refusal |

❌ **未覆盖题型：['clarify', 'compare', 'contrast', 'figure_qa', 'readout', 'regime_trap', 'trend']**

## 二、构成汇总

- **题型**：calc × 5　concept × 5　choice × 2　term × 1　unanswerable × 1
- **子方向**：ambiguity × 4　resolution × 3　pulse_compression × 3　doppler × 2　misc × 2
- **体制**：universal × 10　airborne_pulse × 4
- **难度**：medium × 8　easy × 3　hard × 3
- **问法**：explicit × 11　parametric × 2　correct × 1
- **模态**：text × 14
- **universal 占比**：10/14 = 71%（框架要求 ≥ 30%）
- **token 估算**：min 103 / max 208 / 均值 140（输入预算 1536）
- **视觉样本**：0 条（真值来源 = **人工核对 L3**，判分走 keypoints 覆盖）

## 三、逐条明细

### 1. `txt_000411`　数值计算（calc）

`subdomain=resolution`　`regime=universal`　`difficulty=easy`　`modality=text`　`ask_style=explicit`

`tokens=149`（文本 149 + 图像 0，图 0 张）

**问（user）**

> 雷达的距离分辨率怎么算？以信号带宽 B = 150 MHz 为例说明。

**答（assistant，模型实际会看到的内容）**

```text
公式：\Delta R = \frac{c}{2B}
代入：\Delta R = \frac{3\times10^{8}}{2 \times 1.5\times10^{8}} = 1\ \text{m}
最终答案：1 m
```

**判分**：`{"type": "numeric", "value": 1.0, "unit": "m", "tol": 0.01}`

**出处**：《机载雷达系统与信息处理》第2章 2.4.1（PDF p82）
> 雷达的距离分辨率由雷达发射信号的带宽决定。

**出题说明（评审要点）**：与 v1 `txt_000302`（B = 4 GHz → 3.75 cm）是**同一公式、不同量级**： 本条换成米级（B = 150 MHz → 1.0 m），用来打散"看到分辨率就想到毫米波"的量级捷径。 教材这句话只说"由带宽决定"，**没有给 ΔR = c/(2B) 的符号式** —— 该式来自毫米波语料 （`fmcw.md`："两个目标可分辨的最小距离差由信号带宽决定 ΔR = c/(2B)"），属双信源支撑。

### 2. `txt_000412`　数值计算（calc）

`subdomain=doppler`　`regime=universal`　`difficulty=easy`　`modality=text`　`ask_style=explicit`

`tokens=145`（文本 145 + 图像 0，图 0 张）

**问（user）**

> 雷达的多普勒分辨率怎么算？以相干处理时间 T = 20 ms 为例说明。

**答（assistant，模型实际会看到的内容）**

```text
公式：\Delta f_d = \frac{1}{T_{\mathrm{CPI}}}
代入：\Delta f_d = \frac{1}{0.02} = 50\ \text{Hz}
最终答案：50 Hz
```

**判分**：`{"type": "numeric", "value": 50.0, "unit": "Hz", "tol": 0.01}`

**出处**：《机载雷达系统与信息处理》第2章 2.4.1（PDF p82）
> 雷达的多普勒频率分辨率由雷达信号的持续时间决定。持续时间越长，分辨率越高。

**出题说明（评审要点）**：与 v1 `txt_000304`（T_CPI = 2 ms → 500 Hz）同一公式、不同参数（20 ms → 50 Hz）。 教材还给了实现途径：'这种长时间的要求可以通过发射持续时间很长的脉冲串或连续波， 或通过对多个脉冲的相参积累等来实现' —— 这句话是第 4 章相参积累题的伏笔。

### 3. `txt_000413`　数值计算（calc）

`subdomain=pulse_compression`　`regime=airborne_pulse`　`difficulty=medium`　`modality=text`　`ask_style=parametric`

`tokens=208`（文本 208 + 图像 0，图 0 张）

**问（user）**

> 机载雷达发射线性调频信号时，脉冲压缩带来的信噪比增益怎么算？以脉冲宽度 T = 20 μs、信号带宽 B = 100 MHz 为例，结果用 dB 表示。

**答（assistant，模型实际会看到的内容）**

```text
公式：G_{\mathrm{pc,dB}} = 10\log_{10}(BT)
代入：G_{\mathrm{pc,dB}} = 10\log_{10}(1\times10^{8} \times 2\times10^{-5}) = 33.01\ \text{dB}
最终答案：33.01 dB
```

**判分**：`{"type": "numeric", "value": 33.01029995663981, "unit": "dB", "tol": 0.01}`

**出处**：《机载雷达系统与信息处理》第2章 2.4.3（PDF p89–92）
> 线性调频信号的脉冲压缩处理可以获得大的时宽带宽积。

**出题说明（评审要点）**：G = 10·log10(BT) = 10·log10(2000) ≈ 33.0 dB（v1 `txt_000305` 是 50 → 17.0 dB）。 regime=airborne_pulse：教材是《机载雷达》，LFM 在本章即置于机载语境下讨论； 题面含"机载"表面词，满足 E_REGIME_SURFACE。

### 4. `txt_000414`　数值计算（calc）

`subdomain=doppler`　`regime=airborne_pulse`　`difficulty=medium`　`modality=text`　`ask_style=parametric`

`tokens=167`（文本 167 + 图像 0，图 0 张）

**问（user）**

> 机载雷达探测径向速度为 v = 100 m/s 的目标时，回波的多普勒频移怎么算？以发射波长 λ = 3 cm 为例说明。

**答（assistant，模型实际会看到的内容）**

```text
公式：f_d = \frac{2v}{\lambda}
代入：f_d = \frac{2 \times 100}{0.03} = 6667\ \text{Hz}
最终答案：6667 Hz
```

**判分**：`{"type": "numeric", "value": 6666.666666666667, "unit": "Hz", "tol": 0.01}`

**出处**：GitHub matreshka15/mmWave_Insight docs/mmwave/fmcw.md（速度测量一节）
> 当目标有径向速度时，会产生多普勒频移 f_d = 2v_r/λ

**出题说明（评审要点）**：f_d = 2×100/0.03 ≈ 6666.7 Hz。与 v1 `txt_000306`（车载 FMCW、λ=3.9 mm、v=30 m/s） 构成**同公式跨体制对照**：一个机载 X 波段、一个车载毫米波，量级差两个数量级。

### 5. `txt_000423`　数值计算（calc）

`subdomain=resolution`　`regime=universal`　`difficulty=easy`　`modality=text`　`ask_style=explicit`

`tokens=161`（文本 161 + 图像 0，图 0 张）

**问（user）**

> 不做脉冲压缩时，简单脉冲雷达的距离分辨率怎么算？以脉冲宽度 τ = 1 μs 为例说明。

**答（assistant，模型实际会看到的内容）**

```text
公式：\Delta R = \frac{c\tau}{2}
代入：\Delta R = \frac{3\times10^{8} \times 1\times10^{-6}}{2} = 150\ \text{m}
最终答案：150 m
```

**判分**：`{"type": "numeric", "value": 150.0, "unit": "m", "tol": 0.01}`

**出处**：《机载雷达系统与信息处理》第2章 2.3.3（PDF p79，模糊函数的物理意义）
> 可知雷达能够在何种程度上将两个距离上相差 ΔR = cτ/2、径向速度上相差 Δν = λ f_D/2 的目标分开

**出题说明（评审要点）**：⭐ **与 `txt_000411` 构成第2章最重要的一组对照**：同一个"距离分辨率"， 简单脉冲用 **ΔR = cτ/2**（τ=1 μs → 150 m），脉压信号用 **ΔR = c/(2B)**（B=150 MHz → 1 m）。 两者通过 B ≈ 1/τ 连起来 —— 这正是"**用带宽换距离分辨率**"的全部含义， 也是 `txt_000419`（窄脉冲 → 频谱宽 → 带宽大）那条链的终点。 ⚠️ **本条推翻了我初判的"教材没给 cτ/2"**：p79 原文明确写了这个式子（见文件头记录）。 教训：判断"语料有没有"必须**查过**，不能凭印象 —— 否则会白白丢掉好素材。

### 6. `txt_000415`　概念简答（concept）

`subdomain=pulse_compression`　`regime=universal`　`difficulty=hard`　`modality=text`　`ask_style=explicit`

`tokens=117`（文本 117 + 图像 0，图 0 张）

**问（user）**

> 匹配滤波器输出的最大信噪比取决于什么？

**答（assistant，模型实际会看到的内容）**

```text
由 SNR_max = 2E/N0 可知，它只取决于信号能量 E 与噪声单边功率谱密度 N0，与信号的具体波形无关。
```

**判分**：`{"type": "keyword", "value": "由 SNR_max = 2E/N0 可知，它只取决于信号能量 E 与噪声单边功率谱密度 N0，与信号的具体波形无关。", "keypoints": ["2E", "N0", "波形无关"]}`

**出处**：《机载雷达系统与信息处理》第2章 2.2.4（PDF p76，式 2.2.29）
> 输出 SNR 在 t = T 时刻最大……SNR_max = 2E/N0

**出题说明（评审要点）**：⚠️ **"与波形无关"是从式(2.2.29)读出来的结论，不是教材的原话** —— 式子里只有 E 与 N0， 不含任何波形参数。这类"读公式得出的结论"我会在 notes 里标明推理性质，不冒充原文。 这道题的价值：它解释了**为什么"提高发射能量"是通用的抗噪手段**，而换波形不是。

### 7. `txt_000416`　概念简答（concept）

`subdomain=ambiguity`　`regime=universal`　`difficulty=medium`　`modality=text`　`ask_style=explicit`

`tokens=108`（文本 108 + 图像 0，图 0 张）

**问（user）**

> 雷达信号的模糊函数描述的是什么？

**答（assistant，模型实际会看到的内容）**

```text
模糊函数是两个目标回波信号复包络的时间-频率复合自相关函数，用来描述相邻目标在距离与速度上的可分辨能力。
```

**判分**：`{"type": "keyword", "value": "模糊函数是两个目标回波信号复包络的时间-频率复合自相关函数，用来描述相邻目标在距离与速度上的可分辨能力。", "keypoints": ["复合自相关函数", "可分辨能力"]}`

**出处**：《机载雷达系统与信息处理》第2章 2.3.4（PDF p80）
> χ(τ, f_D) 是两个目标回波信号复包络的时间-频率复合自相关函数。

**出题说明（评审要点）**："描述在距离与速度上的可分辨能力"这句是教材 2.4 开篇对分辨率的定义的归纳 （p82 附近："……普勒频率（或径向速度）上的可分辨能力"），属**同节归纳**而非逐字引用。 模糊函数是 D6 子方向的六大知识点之一，v0/v1 只出过两道**读图题**（vis 矩形脉冲/LFM 模糊函数图）， 本条是第一次正面问它的**定义**。

### 8. `txt_000417`　选择（choice）

`subdomain=ambiguity`　`regime=universal`　`difficulty=medium`　`modality=text`　`ask_style=explicit`

`tokens=131`（文本 131 + 图像 0，图 0 张）

**问（user）**

> 雷达信号的模糊函数按形状大致可分为哪三类？

**答（assistant，模型实际会看到的内容）**

```text
解析：教材把大多数雷达信号的模糊函数按形状分为刀刃形、图钉形和钉板形三类，故 B 正确。A、C、D 都不是教材采用的分类名称。
最终答案：B
```

**判分**：`{"type": "choice", "value": "B", "keypoints": ["刀刃形", "钉板形"]}`

**出处**：《机载雷达系统与信息处理》第2章 2.4（PDF p82）
> 大多数雷达信号波形具有的模糊函数大致可分三类：刀刃形、图钉形和钉板形。

**出题说明（评审要点）**：纯**记忆型**选择题，出它是有意的：分类名称属于"必须照教材说"的硬事实， 模型不能自己造词。干扰项都用了"看起来像分类"的名字（圆形/高斯/单峰）， 考查的是**是否真的读过这段**，而不是推理能力。

### 9. `txt_000424`　选择（choice）

`subdomain=ambiguity`　`regime=universal`　`difficulty=hard`　`modality=text`　`ask_style=correct`

`tokens=169`（文本 169 + 图像 0，图 0 张）

**问（user）**

> 某同学说：「模糊函数的体积是与信号形式无关的常量，所以设计雷达波形没有意义，怎么设计都一样。」下列说法正确的是？

**答（assistant，模型实际会看到的内容）**

```text
解析：教材明确指出体积不变并不代表信号不需要设计：体积虽然不变，但模糊函数图的分布随信号形式而变，其潜在分辨率、理论测量精度及环境适应能力也随之改变，可以通过改变调制特性来改变模糊曲面的形状，故 B 正确。
最终答案：B
```

**判分**：`{"type": "choice", "value": "B", "keypoints": ["体积不变", "分布"]}`

**出处**：《机载雷达系统与信息处理》第2章 2.3.3（PDF p79）
> 虽然体积不变，但由于信号形式不同，模糊函数图的分布也不同，因此其潜在分辨率理论测量精度及环境适应能力等因素也随之改变，可以通过改变雷达信号的调制特性来改变模糊曲面的形状

**出题说明（评审要点）**：**本条是为消除 `W_NO_SHORTCUT_BREAKER` 而补的**（框架告警：本批缺 implicit/correct/contrast 问法， 模型可能学成"看到体制词就套固定公式"）。选这个知识点是有理由的： "体积不变 → 波形设计无意义"是一个**从真前提推出假结论**的推理， 纠错必须同时答出"体积确实不变（所以 C 错）"和"但分布可变（所以 A 错）"两层 —— 单层答对不算对。

### 10. `txt_000418`　概念简答（concept）

`subdomain=resolution`　`regime=universal`　`difficulty=medium`　`modality=text`　`ask_style=explicit`

`tokens=123`（文本 123 + 图像 0，图 0 张）

**问（user）**

> 雷达的距离分辨率与多普勒分辨率分别由什么决定？

**答（assistant，模型实际会看到的内容）**

```text
距离分辨率由发射信号的带宽决定，带宽可以是瞬时带宽或脉压合成带宽；多普勒分辨率由信号的持续时间决定，持续时间越长分辨率越高。
```

**判分**：`{"type": "keyword", "value": "距离分辨率由发射信号的带宽决定，带宽可以是瞬时带宽或脉压合成带宽；多普勒分辨率由信号的持续时间决定，持续时间越长分辨率越高。", "keypoints": ["带宽决定", "持续时间决定"]}`

**出处**：《机载雷达系统与信息处理》第2章 2.4.1（PDF p82，式 2.3.22–2.3.28 的四条结论）
> （1）雷达的距离分辨率由雷达发射信号的带宽决定。（2）雷达的多普勒频率分辨率由雷达信号的持续时间决定。持续时间越长，分辨率越高。

**出题说明（评审要点）**：教材明确说这几条结论"具有普遍意义"，所以 regime=universal 是**教材自己给的**， 不是我的判断。这两条正是 D6 子方向里"分辨率"这一知识点的完整表述 —— 两个维度、 两个不同的决定量，模型最容易把它们混成一个（"分辨率都由带宽决定"）。

### 11. `txt_000419`　概念简答（concept）

`subdomain=pulse_compression`　`regime=airborne_pulse`　`difficulty=medium`　`modality=text`　`ask_style=explicit`

`tokens=137`（文本 137 + 图像 0，图 0 张）

**问（user）**

> 机载雷达发射单频矩形脉冲时，它的频谱宽度由什么决定？

**答（assistant，模型实际会看到的内容）**

```text
单频矩形脉冲的频谱为辛格函数形状，第一个过零点在 1/t_p 处，因此频谱宽度由脉冲宽度 t_p 决定，脉冲越窄频谱越宽。
```

**判分**：`{"type": "keyword", "value": "单频矩形脉冲的频谱为辛格函数形状，第一个过零点在 1/t_p 处，因此频谱宽度由脉冲宽度 t_p 决定，脉冲越窄频谱越宽。", "keypoints": ["辛格函数", "脉冲宽度"]}`

**出处**：《机载雷达系统与信息处理》第2章 2.4.1（PDF p82）
> 单频矩形脉冲的频谱是将矩形脉冲的频谱搬移到射频频率 f_c 处的连续谱线，第一个过零点在 1/t_p 处。

**出题说明（评审要点）**：这是"窄脉冲 = 大带宽"这条链条的**中间一环**，教材走完了全程： t_p → 频谱宽度 1/t_p → 带宽 → 距离分辨率。 ⚠️ **更正**：我初稿在这里写"教材没给 ΔR = cτ/2，所以不出计算题" —— 取证 p79 后发现**教材给了** （见 `txt_000423`）。判断"语料有没有"必须查过再写，不能凭印象。

### 12. `txt_000420`　术语（term）

`subdomain=misc`　`regime=universal`　`difficulty=medium`　`modality=text`　`ask_style=explicit`

`tokens=103`（文本 103 + 图像 0，图 0 张）

**问（user）**

> 匹配滤波是什么？

**答（assistant，模型实际会看到的内容）**

```text
匹配滤波器的脉冲响应是发射信号复共轭的镜像延迟，其输出信噪比在信号结束时刻达到最大。
```

**判分**：`{"type": "keyword", "value": "匹配滤波器的脉冲响应是发射信号复共轭的镜像延迟，其输出信噪比在信号结束时刻达到最大。", "keypoints": ["复共轭的镜像延迟", "输出信噪比"]}`

**出处**：《机载雷达系统与信息处理》第2章 2.2.3（PDF p75）、2.2.4（PDF p76）
> [p75] 由此可知，脉冲响应是发射信号复共轭的镜像延迟。[p76] 输出 SNR 在 t = T 时刻最大。

**出题说明（评审要点）**：正向术语问法（D-P2-13 ③）。**答案只取教材真实给出的两句**，不替它补 "白噪声下最优线性滤波器"这类教材没写的定性 —— 与 v1 `txt_000315`（MUSIC）同一套纪律。 与 `txt_000415` 分工：那条问"最大信噪比取决于什么"（结论），本条问"匹配滤波是什么"（定义）。

### 13. `txt_000421`　概念简答（concept）

`subdomain=ambiguity`　`regime=universal`　`difficulty=medium`　`modality=text`　`ask_style=explicit`

`tokens=132`（文本 132 + 图像 0，图 0 张）

**问（user）**

> 理想的雷达波形，其模糊函数应该是什么形状？现实中为什么做不到？

**答（assistant，模型实际会看到的内容）**

```text
理想的模糊函数应是一个二维冲激函数，这样雷达既没有延时模糊也不存在多普勒模糊；但现实中没有任何波形能做到，实际的模糊函数只能取刀刃形、图钉形或钉板形。
```

**判分**：`{"type": "keyword", "value": "理想的模糊函数应是一个二维冲激函数，这样雷达既没有延时模糊也不存在多普勒模糊；但现实中没有任何波形能做到，实际的模糊函数只能取刀刃形、图钉形或钉板形。", "keypoints": ["二维", "冲激函数", "没有延时模糊"]}`

**出处**：《机载雷达系统与信息处理》第2章 2.4（PDF p82）
> 理想的雷达波形，其模糊函数应具有如图 2.4.1 所示的形状，即一个二维δ冲激函数。此时，雷达既没有延时模糊，又不存在多普勒频率模糊。但实际上没有任何现实的波形能够得到这样的模糊函数。

**出题说明（评审要点）**：与 `txt_000417` 配对：**理想（二维 δ）→ 现实（三类形状）**，教材这一段的论证结构很完整， 合起来才能说明"波形设计到底在权衡什么"。这是第2章最值得出概念题的一处。

### 14. `txt_000422`　不可答（拒答）（unanswerable）

`subdomain=misc`　`regime=airborne_pulse`　`difficulty=hard`　`modality=text`　`ask_style=explicit`

`tokens=123`（文本 123 + 图像 0，图 0 张）

**问（user）**

> 《机载雷达系统与信息处理》第2章在推导匹配滤波器的输出最大信噪比时，所取的信号幅度 A 与噪声单边功率谱密度 N0 分别是多少？

**答（assistant，模型实际会看到的内容）**

```text
资料中未找到相关内容，无法回答。
```

**判分**：`{"type": "refusal", "value": "资料中未找到相关内容，无法回答。"}`

**出处**：《机载雷达系统与信息处理》第2章 2.2.4（PDF p76，式 2.2.25–2.2.29）
> 该节的推导全程使用符号（E = A²T、SNR_max = 2E/N0），教材从未给 A 或 N0 代入任何具体数值。

**出题说明（评审要点）**：**为什么这条是"硬"幻觉探针**：第2章整章都在做符号推导，但式子里有大量字母（A、T、N0、K、t_p）， 模型极易把"见过这些符号"误记成"见过它们的数值"，从而编出"N0 = 4×10⁻²¹ W/Hz"这类看似专业的数。 与 v1 `txt_000319`（AN/APG-68）同一类：**看起来就在书里，其实书里没有**。 ⚠️ 本条 evidence 是**核查结论**而非原文摘句（教材没有可摘的"没有"），与 v1 `txt_000320` 的处理一致。
