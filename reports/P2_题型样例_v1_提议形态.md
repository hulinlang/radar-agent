# P2 题型样例 v1（提议形态 · 24 条 / 12 类任务）

> 数据源：`logs/probe/_v1_out/p2_alltasks_v1.jsonl`　|　共 **24** 条
> 
> 语料：《机载雷达系统与信息处理》（电子工业出版社 2021.8，ISBN 978-7-121-41746-7）
> ＋ GitHub `matreshka15/mmWave_Insight`（Markdown 文档站，21 篇）
> 
> ⚠️ 本文件由 `scripts/p2_render_samples.py` **从编译产物生成**，不是手写；
> 因此下面展示的答案与将来模型看到的是**同一份字节**。
> ⚠️ 页码口径：一律 **PDF 页序（1-based）**，与书内印刷页码不同。

## 一、题型覆盖率（spec 里登记的**全部**题型）

| 题型 | 中文 | 条数 | 判分方式 |
|---|---|---|---|
| `calc` | 数值计算 | 7 ✅ | numeric |
| `choice` | 选择 | 2 ✅ | choice |
| `clarify` | 澄清 | 1 ✅ | keyword |
| `compare` | 图内对比 | 1 ✅ | keyword |
| `concept` | 概念简答 | 3 ✅ | keyword |
| `contrast` | 双体制对比 | 1 ✅ | keyword |
| `figure_qa` | 看图理解 | 1 ✅ | keyword |
| `readout` | 读图取值 | 1 ✅ | keyword |
| `regime_trap` | 体制陷阱 | 1 ✅ | choice |
| `term` | 术语 | 3 ✅ | keyword |
| `trend` | 读图趋势 | 1 ✅ | keyword |
| `unanswerable` | 不可答（拒答） | 2 ✅ | refusal |

✅ **12 类题型全覆盖**（24 条）

## 二、构成汇总

- **题型**：calc × 7　concept × 3　term × 3　choice × 2　unanswerable × 2　regime_trap × 1　contrast × 1　clarify × 1　figure_qa × 1　readout × 1　trend × 1　compare × 1
- **子方向**：misc × 8　ambiguity × 6　resolution × 5　doppler × 3　pulse_compression × 2
- **体制**：universal × 10　airborne_pulse × 7　fmcw × 4　automotive_mmwave × 2　unspecified × 1
- **难度**：medium × 13　hard × 8　easy × 3
- **问法**：explicit × 16　implicit × 2　parametric × 2　correct × 2　contrast × 1　clarify × 1
- **模态**：text × 20　vision × 4
- **universal 占比**：10/24 = 42%（框架要求 ≥ 30%）
- **token 估算**：min 95 / max 481 / 均值 194（输入预算 1536）
- **视觉样本**：4 条（真值来源 = **人工核对 L3**，判分走 keypoints 覆盖）

## 三、逐条明细

### 1. `txt_000301`　数值计算（calc）

`subdomain=doppler`　`regime=airborne_pulse`　`difficulty=medium`　`modality=text`　`ask_style=explicit`

`tokens=169`（文本 169 + 图像 0，图 0 张）

**问（user）**

> 机载脉冲多普勒雷达的最大不模糊速度是怎么算出来的？以发射波长 λ = 3 cm、脉冲重复频率 PRF = 10 kHz 为例说明。

**答（assistant，模型实际会看到的内容）**

```text
公式：v_u = \frac{\lambda\,\mathrm{PRF}}{4}
代入：v_u = \frac{0.03 \times 10000}{4} = 75\ \text{m/s}
最终答案：75 m/s
```

**判分**：`{"type": "numeric", "value": 75.0, "unit": "m/s", "tol": 0.01}`

**出处**：《机载雷达系统与信息处理》第4章 4.7（PDF p166–174，PD 雷达三种 PRF 模式）
> 低 PRF 模式下杂波与目标的多普勒剖面分布不同，PRF 的选择直接决定了不模糊速度与不模糊距离的折中。

**出题说明（评审要点）**：**① 的核心：合并不改答案、只改题面**。题面从"某雷达…求 v_u"（作业口吻）改成用户的自然问法 "…是怎么算出来的？"，而答案形态**一字未动** —— 仍是「公式 / 代入 / 最终答案」三行（程序生成）。 对比对象：v0 的 `txt_000205`。示例参数由我们给，但**代入值与结果一律由 formulas.py 算**，不手写。 与 `txt_000303` 构成 R_u / v_u 对子（同一句"PRF 决定不模糊 X"，两个体制口径不同）。 ⚠️ 未挂 term_key：`unambiguous_velocity` 尚未进 `term_registry`（D-P2-12）。

### 2. `txt_000302`　数值计算（calc）

`subdomain=resolution`　`regime=fmcw`　`difficulty=easy`　`modality=text`　`ask_style=implicit`

`tokens=162`（文本 162 + 图像 0，图 0 张）

**问（user）**

> 车载调频连续波雷达的距离分辨率是怎么算的？以扫频带宽 B = 4 GHz 为例说明。

**答（assistant，模型实际会看到的内容）**

```text
公式：\Delta R = \frac{c}{2B}
代入：\Delta R = \frac{3\times10^{8}}{2 \times 4\times10^{9}} = 0.0375\ \text{m}
最终答案：0.0375 m
```

**判分**：`{"type": "numeric", "value": 0.0375, "unit": "m", "tol": 0.01}`

**出处**：GitHub matreshka15/mmWave_Insight docs/mmwave/fmcw.md（距离分辨率一节）
> 两个目标可分辨的最小距离差由信号带宽决定 ΔR = c/(2B)

**出题说明（评审要点）**：对比对象：v0 的 `txt_000201`（知识点完全相同，只改题面口吻）。 ask_style=implicit：题面不出现 "FMCW" 字样，靠"车载调频连续波 / 扫频带宽"限定体制。 数值交叉验证：mmWave 文档自己的对照表给出 B = 4 GHz → 3.75 cm，与程序算出的 c/(2×4e9) **一致**。 **同一公式两体制**：与 `txt_000317`（contrast）呼应 —— 式子是 ΔR = c/(2B)，但 B 的含义不同。

### 3. `txt_000303`　数值计算（calc）

`subdomain=ambiguity`　`regime=airborne_pulse`　`difficulty=medium`　`modality=text`　`ask_style=explicit`

`tokens=163`（文本 163 + 图像 0，图 0 张）

**问（user）**

> 机载脉冲雷达的最大不模糊距离该怎么算？以脉冲重复频率 PRF = 10 kHz 为例说明。

**答（assistant，模型实际会看到的内容）**

```text
公式：R_u = \frac{c}{2\,\mathrm{PRF}}
代入：R_u = \frac{3\times10^{8}}{2 \times 10000} = 15000\ \text{m}
最终答案：15000 m
```

**判分**：`{"type": "numeric", "value": 15000.0, "unit": "m", "tol": 0.01}`

**出处**：《机载雷达系统与信息处理》第4章 4.3.1.2（PDF p142–149，最大不模糊距离）
> 能够收到的单次往返时间回波的最大距离称为最大不模糊距离……因为其脉冲往返时间等于脉冲重复周期，所以 R_u = c T_r / 2

**出题说明（评审要点）**：**★ 本条是新增，不是 v0 改造**：`formula` 并入 calc 后需要一条"问公式 + 给参数"的样本， 而 R_u 侧在 v0 里**只有陷阱项引用过、从没有正面教过**（见 v0 `txt_000214` 的 A 选项）。 教材用脉冲重复周期 T_r 表述（T_r = 1/PRF），与本条程序用的 c/(2·PRF) 等价。 数值交叉验证：3e8 / (2 × 10^4) = 15000 m。 ⚠️ 本条**替代**了 v0 的 `txt_000207`（原 formula：FMCW 测距式 R = c·f_beat·T_c/(2B)）—— 那个式子**不在** `formulas.py` 注册表里，无法作为 calc 出题。缺口已记入报告，待定夺。

### 4. `txt_000304`　数值计算（calc）

`subdomain=doppler`　`regime=universal`　`difficulty=medium`　`modality=text`　`ask_style=explicit`

`tokens=149`（文本 149 + 图像 0，图 0 张）

**问（user）**

> 雷达的多普勒分辨率怎么算？以相干处理时间 T_CPI = 2 ms 为例说明。

**答（assistant，模型实际会看到的内容）**

```text
公式：\Delta f_d = \frac{1}{T_{\mathrm{CPI}}}
代入：\Delta f_d = \frac{1}{0.002} = 500\ \text{Hz}
最终答案：500 Hz
```

**判分**：`{"type": "numeric", "value": 500.0, "unit": "Hz", "tol": 0.01}`

**出处**：《机载雷达系统与信息处理》第2章 2.4.1（PDF p82）
> 雷达的多普勒频率分辨率由雷达信号的持续时间决定。持续时间越长，分辨率越高。

**出题说明（评审要点）**：题面**刻意不含任何体制词**（regime=universal）—— "多普勒分辨率 = 1/T_CPI" 是普适结论， 写成"某脉冲多普勒雷达…"反而会触发 W_REGIME_MISMATCH。 这正是"先定知识层、再定体制层"的体现（regime 与 subdomain 正交）。

### 5. `txt_000305`　数值计算（calc）

`subdomain=pulse_compression`　`regime=universal`　`difficulty=medium`　`modality=text`　`ask_style=parametric`

`tokens=200`（文本 200 + 图像 0，图 0 张）

**问（user）**

> 线性调频信号的脉冲压缩信噪比增益怎么算？以脉冲宽度 T = 10 μs、信号带宽 B = 5 MHz 为例，结果用 dB 表示。

**答（assistant，模型实际会看到的内容）**

```text
公式：G_{\mathrm{pc,dB}} = 10\log_{10}(BT)
代入：G_{\mathrm{pc,dB}} = 10\log_{10}(5\times10^{6} \times 1\times10^{-5}) = 16.99\ \text{dB}
最终答案：16.99 dB
```

**判分**：`{"type": "numeric", "value": 16.98970004336019, "unit": "dB", "tol": 0.01}`

**出处**：《机载雷达系统与信息处理》第2章 2.4.3（PDF p89–92）
> 线性调频信号的脉冲压缩处理可以获得大的时宽带宽积。

**出题说明（评审要点）**：G = 10·log10(BT) = 10·log10(50) ≈ 17.0 dB。**注意量纲**：本条问的是 dB； 若要"倍"，应改用 `pulse_compression_gain`（输出单位"倍"）。 框架有 E_UNIT_MISMATCH 断言 —— 雷达题最容易在 dB/倍、km/m 上翻车。

### 6. `txt_000306`　数值计算（calc）

`subdomain=doppler`　`regime=fmcw`　`difficulty=medium`　`modality=text`　`ask_style=parametric`

`tokens=177`（文本 177 + 图像 0，图 0 张）

**问（user）**

> 车载调频连续波雷达里，目标的径向速度与回波的多普勒频移是什么关系？以发射波长 λ = 3.9 mm、径向速度 v = 30 m/s 为例说明。

**答（assistant，模型实际会看到的内容）**

```text
公式：f_d = \frac{2v}{\lambda}
代入：f_d = \frac{2 \times 30}{0.0039} = 15380\ \text{Hz}
最终答案：15380 Hz
```

**判分**：`{"type": "numeric", "value": 15384.615384615385, "unit": "Hz", "tol": 0.01}`

**出处**：GitHub matreshka15/mmWave_Insight docs/mmWave/fmcw.md（速度测量一节）
> 当目标有径向速度时，会产生多普勒频移 f_d = 2v_r/λ

**出题说明（评审要点）**：对比对象：v0 的 `txt_000202`。原来的问法是"某调频连续波雷达…求 f_d"， 现在改成"…是什么关系？"，同时保留 λ 用 mm 给、需转成 m 的考点（ask_style=parametric）。 本条的多普勒式**两份语料都有原式**（教材与 mmWave 均有），出处是硬的。

### 7. `txt_000307`　数值计算（calc）

`subdomain=misc`　`regime=universal`　`difficulty=easy`　`modality=text`　`ask_style=implicit`

`tokens=163`（文本 163 + 图像 0，图 0 张）

**问（user）**

> 已知雷达的工作频率，它的工作波长怎么算？以 f0 = 35 GHz 为例说明。

**答（assistant，模型实际会看到的内容）**

```text
公式：\lambda = \frac{c}{f_0}
代入：\lambda = \frac{3\times10^{8}}{3.5\times10^{10}} = 0.008571\ \text{m}
最终答案：0.008571 m
```

**判分**：`{"type": "numeric", "value": 0.008571428571428572, "unit": "m", "tol": 0.01}`

**出处**：《机载雷达系统与信息处理》第1章 1.1（PDF p12）
> 对于许多舰载和地基雷达采用的较长波长（对应较低的频率）来说，大气几乎是完全透明的

**出题说明（评审要点）**：⚠️ **本条证据强度：中 —— 需要你判断是否可接受**。λ = c/f0 属电磁波基本关系， 但**两份语料都没有单列这个定义式**：教材只在第 1 章建立了"较长波长 ↔ 较低频率"的对应， 毫米波术语表则在多普勒式 f_d = 2v_r/λ 里出现过 λ。 本条 evidence 取教材那句（建立波长-频率关系），**公式本身属物理常识、而非语料结论**。 若评审要求"每一条公式都必须有语料出处"，**本条应剔除**。 （同类风险：`coherent_integration_gain_db`，v0 `txt_000102` 已声明过。） 数值：λ = 3e8 / 3.5e10 ≈ 8.57 mm。题面只说频率、不提体制（频率不是体制）。

### 8. `txt_000308`　选择（choice）

`subdomain=ambiguity`　`regime=fmcw`　`difficulty=medium`　`modality=text`　`ask_style=correct`

`tokens=209`（文本 209 + 图像 0，图 0 张）

**问（user）**

> 某同学说：「连续波雷达没有脉冲重复周期，所以它的最大可测距离不受任何限制，只要有足够的发射功率就行。」下列说法正确的是？

**答（assistant，模型实际会看到的内容）**

```text
解析：连续波体制没有脉冲重复周期，其最大可测距离由 ADC 采样率与 Chirp 周期共同决定：拍频不能超过采样率的一半（奈奎斯特），故 B 正确。A 错在把最大距离归结为发射功率，忽略了采样率与时间的限制；C 是体制错配，脉冲重复频率在连续波体制里根本不存在；D 与最大距离无直接关系。
最终答案：B
```

**判分**：`{"type": "choice", "value": "B", "keypoints": ["没有脉冲重复周期", "采样率的一半"]}`

**出处**：GitHub matreshka15/mmWave_Insight docs/mmwave/fmcw.md（最大探测距离 / 为什么有最大距离限制）
> 原因：拍频不能超过采样率的一半（奈奎斯特）R_max = c·f_s·T_c/(4B)

**出题说明（评审要点）**：**② 的理由**：SFT 学的是 P(输出|输入)。训练样本的 assistant 只有字母，模型学到的就只有字母 —— 不是"模型不肯解释"，而是**没教**。对比对象：v0 的 `txt_000208`（只有 `最终答案：B`）。 判分**不变**（仍是 choice 精确匹配字母）；`keypoints` 是**第二层**， 兜住"解析与字母不一致"这种会被精确匹配静默放过的问题。 ask_style=correct（纠错式）：先给一个**听起来合理**的错误结论，再要求指出错在哪。 ⚠️ 与 `txt_000316`（regime_trap）考点相同（"连续波最大距离"）：v0 的既有设计（同一知识点两题型）， 本条解析侧重"为什么 A（发射功率）错"，316 侧重"为什么 A（把脉冲雷达公式搬过来）错"。

### 9. `txt_000309`　选择（choice）

`subdomain=resolution`　`regime=universal`　`difficulty=hard`　`modality=text`　`ask_style=correct`

`tokens=179`（文本 179 + 图像 0，图 0 张）

**问（user）**

> 「想要更高的距离分辨率，就得把发射脉冲做得更窄。」在脉冲压缩体制下，这句话的问题在于？

**答（assistant，模型实际会看到的内容）**

```text
解析：距离分辨率由信号带宽决定，而不是脉冲宽度：脉冲压缩用长脉冲加调制换取大带宽，从而同时获得长脉冲的能量与高距离分辨率，故 B 正确。A 只对未做压缩的简单脉冲成立（那时 ΔR = c·τ/2）；C、D 与距离分辨率无关。
最终答案：B
```

**判分**：`{"type": "choice", "value": "B", "keypoints": ["信号带宽", "长脉冲"]}`

**出处**：《机载雷达系统与信息处理》第2章 2.4.1（PDF p82）
> 雷达的距离分辨率由雷达发射信号的带宽决定。这种带宽可以是瞬时带宽（如极窄的脉冲），也可以是合成的（通过时间换取得到的，如脉冲持续时间长的线性调频波形）。

**出题说明（评审要点）**：教材这句话本身就**同时否掉 A 并解释 B**："带宽可以是瞬时的（窄脉冲），也可以是合成的（长脉冲+调制）"。 A 之所以诱人，是因为它**只对简单脉冲成立**（ΔR = cτ/2）—— "两个公式各对一半"的经典陷阱。 ⚠️ **顺手清掉一处数据卫生问题**：v0 的 `txt_000209` 在 B 选项里混进了 Markdown 强调符 （`**信号带宽**`）—— 那类噪声会**静默进训练集**、教模型输出星号（D-P2-16）。本版已清除。

### 10. `txt_000310`　概念简答（concept）

`subdomain=ambiguity`　`regime=fmcw`　`difficulty=medium`　`modality=text`　`ask_style=explicit`

`tokens=124`（文本 124 + 图像 0，图 0 张）

**问（user）**

> 简述 FMCW 雷达测距的基本原理：为什么它可以用频率测量代替时间测量？

**答（assistant，模型实际会看到的内容）**

```text
发射频率随时间线性变化的连续波，混频得到拍频，拍频正比于目标距离，从而把时间测量转换成频率测量。
```

**判分**：`{"type": "keyword", "value": "发射频率随时间线性变化的连续波，混频得到拍频，拍频正比于目标距离，从而把时间测量转换成频率测量。", "keypoints": ["线性变化", "拍频", "频率测量"]}`

**出处**：GitHub matreshka15/mmWave_Insight docs/mmwave/fmcw.md（测距的关键 / 步骤 3）
> FMCW 雷达巧妙地将时间测量转换为频率测量；f_beat = S·τ = 2SR/c

**出题说明（评审要点）**：形态未变，沿用 v0 `txt_000210`。三个要点都在参考答案里逐字出现 （`E_KEYPOINT_NOT_IN_ANSWER` 是 critical 断言，不满足直接拒收）。 ⚠️ 题面仍是"简述…"的作业口吻 —— 要不要一并改成自然问法，**由你定**（我没擅自扩大改动面）。

### 11. `txt_000311`　概念简答（concept）

`subdomain=resolution`　`regime=universal`　`difficulty=medium`　`modality=text`　`ask_style=explicit`

`tokens=128`（文本 128 + 图像 0，图 0 张）

**问（user）**

> 简述脉冲压缩体制为什么能用较长的脉冲获得较高的距离分辨率。

**答（assistant，模型实际会看到的内容）**

```text
距离分辨率由信号带宽决定而不是脉冲宽度；线性调频等信号通过增大带宽、再用匹配滤波压缩成窄脉冲，因此长脉冲也能获得高分辨率。
```

**判分**：`{"type": "keyword", "value": "距离分辨率由信号带宽决定而不是脉冲宽度；线性调频等信号通过增大带宽、再用匹配滤波压缩成窄脉冲，因此长脉冲也能获得高分辨率。", "keypoints": ["带宽决定", "匹配滤波", "窄脉冲"]}`

**出处**：《机载雷达系统与信息处理》第2章 2.4.1（PDF p82）、2.2.2（PDF p73）
> 雷达的距离分辨率由雷达发射信号的带宽决定……也可以是合成的（通过时间换取得到的，如脉冲持续时间长的线性调频波形）。

**出题说明（评审要点）**：形态未变，沿用 v0 `txt_000211`。**两个知识点的组合拳**：p82 给"分辨率由带宽决定"， p73 给"匹配滤波获得最大输出信噪比"，合起来才能解释"长脉冲为什么能变窄"。 教材没有一句话直接回答本题 —— 这是**抽取式出题**的正常形态，也是 source 要写两处的原因。

### 12. `txt_000312`　概念简答（concept）

`subdomain=misc`　`regime=automotive_mmwave`　`difficulty=hard`　`modality=text`　`ask_style=explicit`

`tokens=146`（文本 146 + 图像 0，图 0 张）

**问（user）**

> 在毫米波 MIMO 雷达中，由多个发射阵元与接收阵元通过波形正交化得到的等效大孔径阵列称为什么？它带来什么直接好处？

**答（assistant，模型实际会看到的内容）**

```text
称为虚拟阵列，等效阵元数为发射阵元数与接收阵元数之积，因此在不增加实体阵元的情况下提高了角度分辨率。
```

**判分**：`{"type": "keyword", "value": "称为虚拟阵列，等效阵元数为发射阵元数与接收阵元数之积，因此在不增加实体阵元的情况下提高了角度分辨率。", "keypoints": ["虚拟阵列", "之积", "角度分辨率"]}`

**出处**：GitHub matreshka15/mmWave_Insight docs/glossary.md（MIMO 雷达 / Virtual Array 条目）
> MIMO 雷达：多发多收雷达，通过正交波形构造虚拟阵列提升角度分辨率。

**出题说明（评审要点）**：形态未变，沿用 v0 `txt_000212`。 ⚠️ **术语碰撞（两份语料最有价值的一处发现）**：同一个词「虚拟阵列」在两份语料里**不是一回事** —— 教材第 8 章（PDF p317）讲的是 **ISAR 的合成阵列**，毫米波语料讲的是 **MIMO 的 N_TX·N_RX 等效阵元**。 这正是 regime 必须显式的理由。 ⚠️ `term_registry` 目前**没有**「虚拟阵列」这一条 —— 放量前应补（D-P2-12）。

### 13. `txt_000313`　术语（term）

`subdomain=misc`　`regime=universal`　`difficulty=easy`　`modality=text`　`ask_style=explicit`

`tokens=114`（文本 114 + 图像 0，图 0 张）

**问（user）**

> CA-CFAR 是什么？

**答（assistant，模型实际会看到的内容）**

```text
CA-CFAR 即均值恒虚警（Cell Averaging CFAR），用待检测单元两侧参考单元的功率平均来估计噪声电平，适用于均匀噪声背景。
```

**判分**：`{"type": "keyword", "value": "CA-CFAR 即均值恒虚警（Cell Averaging CFAR），用待检测单元两侧参考单元的功率平均来估计噪声电平，适用于均匀噪声背景。", "keypoints": ["均值恒虚警", "参考单元", "均匀噪声背景"]}`

**出处**：GitHub matreshka15/mmWave_Insight docs/glossary.md（CA-CFAR 条目）
> CA-CFAR（Cell Averaging CFAR）：均值 CFAR，适用于均匀噪声背景。

**出题说明（评审要点）**：**③ 的核心**：从**反向猜词**（v0 `txt_000213`："把参考单元功率取平均的方法，英文缩写是什么？"） 改成**正向问法**（"CA-CFAR 是什么？"）—— 真实用户几乎不会问前者。 ⚠️ **一个连带后果，而且我认为是对的**：正向题面很短、不含体制表面词 → regime 只能落 `universal`（框架要求 regime≠universal 时题面必须出现体制词）。 术语本身多属普适，体制差异应交给 contrast / regime_trap 去考。

### 14. `txt_000314`　术语（term）

`subdomain=misc`　`regime=airborne_pulse`　`difficulty=medium`　`modality=text`　`ask_style=explicit`

`tokens=121`（文本 121 + 图像 0，图 0 张）

**问（user）**

> 机载雷达里的 STAP 是什么？

**答（assistant，模型实际会看到的内容）**

```text
STAP 即空时自适应处理，在空间与慢时间（多普勒）两个维度上联合自适应处理，用于抑制机载雷达强地杂波的空间谱-多普勒耦合。
```

**判分**：`{"type": "keyword", "value": "STAP 即空时自适应处理，在空间与慢时间（多普勒）两个维度上联合自适应处理，用于抑制机载雷达强地杂波的空间谱-多普勒耦合。", "keypoints": ["空时自适应处理", "联合自适应", "地杂波"]}`

**出处**：《机载雷达系统与信息处理》第7章 7.6.1（PDF p282）
> 在机载雷达中，地杂波强且存在空间谱和多普勒耦合现象，采用空时自适应信号处理（STAP）可以有效地消除这些地杂波。

**出题说明（评审要点）**：与上一条配对，演示**两种都可行**的正向问法： 本题的术语**强绑定体制**（STAP 就是为机载地杂波的空时耦合而生的）， 所以题面写"**机载**雷达里的 STAP 是什么？"—— 这仍是自然问法，同时满足 E_REGIME_SURFACE。 → 结论：正向术语题要**按术语本身是否绑定体制**决定 regime，而不是一刀切。

### 15. `txt_000315`　术语（term）

`subdomain=misc`　`regime=universal`　`difficulty=hard`　`modality=text`　`ask_style=explicit`

`tokens=95`（文本 95 + 图像 0，图 0 张）

**问（user）**

> MUSIC 是什么？

**答（assistant，模型实际会看到的内容）**

```text
MUSIC 是基于噪声子空间正交性的超分辨率来波方向（DOA）估计算法。
```

**判分**：`{"type": "keyword", "value": "MUSIC 是基于噪声子空间正交性的超分辨率来波方向（DOA）估计算法。", "keypoints": ["噪声子空间", "超分辨率"]}`

**出处**：GitHub matreshka15/mmWave_Insight docs/glossary.md（MUSIC 条目）
> MUSIC：基于噪声子空间正交性的超分辨率 DOA 算法。

**出题说明（评审要点）**：第三条术语题，用来对照**"答案几乎等于语料原文"**这一形态：术语题就该这样 —— 答案**只取语料真实给出的部分**，不替它补英文全称、不补算法细节（与 v0 `txt_000107`（MTI）同一套纪律）。 若要考"MUSIC 与 Capon 的差别"，那是放量时的辨析题，不是术语题。

### 16. `txt_000316`　体制陷阱（regime_trap）

`subdomain=ambiguity`　`regime=automotive_mmwave`　`difficulty=hard`　`modality=text`　`ask_style=explicit`

`tokens=208`（文本 208 + 图像 0，图 0 张）

**问（user）**

> 某车载毫米波雷达采用调频连续波体制。关于它的「最大可测距离」受什么限制，下列说法正确的是？

**答（assistant，模型实际会看到的内容）**

```text
解析：连续波体制没有脉冲重复周期，最大可测距离由采样率与 Chirp 周期共同决定：拍频不能超过采样率的一半（奈奎斯特），故 B 正确。A 正是把脉冲雷达的 R_u = c/(2·PRF) 原样搬到连续波体制上，而该体制根本没有脉冲重复周期，物理上不成立；C、D 都不是该体制下最大距离的限制来源。
最终答案：B
```

**判分**：`{"type": "choice", "value": "B", "keypoints": ["没有脉冲重复周期", "奈奎斯特"]}`

**出处**：《机载雷达系统与信息处理》第4章 4.3.1.2（PDF p142–149）；GitHub matreshka15/mmWave_Insight docs/mmwave/fmcw.md
> [脉冲雷达] 因为其脉冲往返时间等于脉冲重复周期，所以 R_u = c T_r / 2；[FMCW] 原因：拍频不能超过采样率的一半（奈奎斯特）R_max = c·f_s·T_c/(4B)

**出题说明（评审要点）**：**本轮加了双信源**：v0 版只有 mmWave 一条 quote，**无法证明陷阱项 A 在脉冲雷达里确实成立** —— 现在补上教材 4.3.1.2 的原文，A 的"合法性"与 B 的"正确性"各有出处，陷阱才站得住。 加上**解析**之后，这道题才真正"教了判据"：v0 只有 `最终答案：B`， 等于告诉模型"看到这段话选 B"，而没告诉它**为什么 A 是错的**。 框架有 E_TRAP_NO_REGIME：regime_trap **不允许**挂在 universal 上。

### 17. `txt_000317`　双体制对比（contrast）

`subdomain=resolution`　`regime=airborne_pulse`　`regime_b=fmcw`　`difficulty=hard`　`modality=text`　`ask_style=contrast`

`tokens=178`（文本 178 + 图像 0，图 0 张）

**问（user）**

> 公式 ΔR = c/(2B) 在机载脉冲雷达与 FMCW 雷达里都成立，但 B 的含义是否相同？请分别说明。

**答（assistant，模型实际会看到的内容）**

```text
形式相同、含义不同：机载脉冲雷达中 B 是发射信号的调制带宽（可由脉冲压缩用时间换取），FMCW 雷达中 B 是 Chirp 的扫频带宽，即一个 Chirp 内频率扫描的总范围；两者都靠增大带宽提高距离分辨率。
```

**判分**：`{"type": "keyword", "value": "形式相同、含义不同：机载脉冲雷达中 B 是发射信号的调制带宽（可由脉冲压缩用时间换取），FMCW 雷达中 B 是 Chirp 的扫频带宽，即一个 Chirp 内频率扫描的总范围；两者都靠增大带宽提高距离分辨率。", "keypoints": ["调制带宽", "扫频带宽", "增大带宽"]}`

**出处**：《机载雷达系统与信息处理》第2章 2.4.1（PDF p82）；GitHub matreshka15/mmWave_Insight docs/mmwave/fmcw.md（距离分辨率）
> [A] 雷达的距离分辨率由雷达发射信号的带宽决定……也可以是合成的（通过时间换取得到的）。[B] 两个目标可分辨的最小距离差由信号带宽决定 ΔR = c/(2B)。

**出题说明（评审要点）**：形态未变，沿用 v0 `txt_000215`。要点：**同一个式子、同一个符号、两个体制下不同的物理含义** —— 光看公式完全看不出差别，这正是"不声明体制就没有唯一口径"的教科书级例子。 ⚠️ 框架断言：regime_b 必填、必须与 regime 不同、且不能是 unspecified。

### 18. `txt_000318`　澄清（clarify）

`subdomain=resolution`　`regime=unspecified`　`difficulty=medium`　`modality=text`　`ask_style=clarify`

`tokens=146`（文本 146 + 图像 0，图 0 张）

**问（user）**

> 某雷达要求距离分辨率达到 0.5 m，请计算它需要的信号带宽。

**答（assistant，模型实际会看到的内容）**

```text
这个问题需要先明确雷达体制：距离分辨率对应的"带宽"随体制而不同，脉冲雷达是调制带宽、连续波雷达是扫频带宽，SAR 方位向分辨率则与带宽无关。请补充所问雷达的体制后再计算。
```

**判分**：`{"type": "keyword", "value": "这个问题需要先明确雷达体制：距离分辨率对应的\"带宽\"随体制而不同，脉冲雷达是调制带宽、连续波雷达是扫频带宽，SAR 方位向分辨率则与带宽无关。请补充所问雷达的体制后再计算。", "keypoints": ["体制", "脉冲雷达", "连续波"]}`

**出处**：《机载雷达系统与信息处理》第8章 8.3.2（PDF p310）；GitHub matreshka15/mmWave_Insight docs/mmwave/fmcw.md
> [A] 合成阵列……所能得到的横向距离分辨单元长度为 D/2，而与目标距离远近无关。[B] 两个目标可分辨的最小距离差由信号带宽决定 ΔR = c/(2B)。

**出题说明（评审要点）**：**④ 的问题不在题，在判分**：clarify 原来落到 `keyword` 且**没有任何 keypoints**， 等于"生成了但判不了"（与 `concept` 明明有 `E_CHECK_KEYPOINTS` 守卫形成刺眼对比）。 补上要点后，"是否真的先澄清、且说清了为什么必须澄清"才可机器判分。 **这道题不澄清就一定做错**：题面给 0.5 m，若默认脉冲雷达会答 B = c/(2×0.5) = 300 MHz； 但若目标是 SAR 方位向，分辨率是 D/2、**跟带宽无关**，答案根本不存在。 框架断言：regime=unspecified ⟺ task=clarify（双向）；answer 必须含"体制"。 ⚠️ 配比必须小（W_CLARIFY_SHARE）—— 多了会让模型对明确的问题也反问。

### 19. `txt_000319`　不可答（拒答）（unanswerable）

`subdomain=misc`　`regime=airborne_pulse`　`difficulty=hard`　`modality=text`　`ask_style=explicit`

`tokens=115`（文本 115 + 图像 0，图 0 张）

**问（user）**

> 《机载雷达系统与信息处理》中提到的 AN/APG-68 型脉冲多普勒雷达，它的发射峰值功率与天线增益分别是多少？

**答（assistant，模型实际会看到的内容）**

```text
资料中未找到相关内容，无法回答。
```

**判分**：`{"type": "refusal", "value": "资料中未找到相关内容，无法回答。"}`

**出处**：《机载雷达系统与信息处理》第1章（PDF p25，图1.2.15）与 1.3.3（PDF p37）
> 根据雷达所担负任务的不同，脉冲功率大小不一，有的可达几兆瓦，有的则为几十千瓦。

**出题说明（评审要点）**：形态未变，沿用 v0 `txt_000217`。**这是"硬"幻觉探针，不是"显然不可答"**： 书名里就有 AN/APG-68（p25 有它的照片），学生完全可能"记得见过"从而编出数字 —— 但教材**只给了量级区间，从未给出该型号的具体参数**。证据句就是证明：教材说的是"范围"而非型号参数。

### 20. `txt_000320`　不可答（拒答）（unanswerable）

`subdomain=misc`　`regime=airborne_pulse`　`difficulty=medium`　`modality=text`　`ask_style=explicit`

`tokens=111`（文本 111 + 图像 0，图 0 张）

**问（user）**

> 《机载雷达系统与信息处理》第7章中给出的某型机载相控阵雷达，它的阵元数量和阵面尺寸分别是多少？

**答（assistant，模型实际会看到的内容）**

```text
资料中未找到相关内容，无法回答。
```

**判分**：`{"type": "refusal", "value": "资料中未找到相关内容，无法回答。"}`

**出处**：《机载雷达系统与信息处理》第7章（PDF p255–296）与第8章 8.1（PDF p297）
> 第7章讨论阵列处理的一般原理（阵列雷达、数字波束形成、STAP），未给出任何具体型号的阵元数量或阵面尺寸；第8章仅以"天线孔径为 300 个波长"举例说明波束宽度。

**出题说明（评审要点）**：形态未变，沿用 v0 `txt_000218`。 **"原理章节里出现的具体数字"是幻觉的高发区**：第8章确实出现过"300 个波长""约 10 m"这类数字， 但那是**教学举例**，不是某个型号的参数 —— 模型很容易把举例数字当型号参数答出来。

### 21. `vis_000301`　看图理解（figure_qa）

`subdomain=pulse_compression`　`regime=universal`　`difficulty=medium`　`modality=vision`　`ask_style=explicit`

`tokens=350`（文本 135 + 图像 215，图 1 张）

**图（864×256，sha256 `d1b7b62919672acd…`）**

![vis_000301](../data_processed/figs_vision/d1b7b62919672acdaaa2169ba534dffc964accd83b0ded7c6ae531c97c0fd34f.jpg)

`figure_truth`：`{"kind": "textbook_figure", "pdf_page": 102, "caption": "图2.5.2 用FFT实现数字脉冲压缩的原理框图", "visible_blocks": ["FFT", "复数乘法器（共轭频谱及加权系数）", "IFFT", "包络检波"], "verified_by": "manual-pending"}`

**问（user）**

> 图中是用 FFT 实现数字脉冲压缩的原理框图。请说明信号从左到右依次经过哪些主要环节。

**答（assistant，模型实际会看到的内容）**

```text
依次经过 FFT、与共轭频谱及加权系数在复数乘法器中相乘、再经 IFFT，最后做包络检波输出。
```

**判分**：`{"type": "keyword", "value": "依次经过 FFT、与共轭频谱及加权系数在复数乘法器中相乘、再经 IFFT，最后做包络检波输出。", "keypoints": ["FFT", "复数乘法器", "IFFT", "包络检波"]}`

**出处**：《机载雷达系统与信息处理》第2章 2.5.2（PDF p102，图2.5.2）
> 图2.5.2 用FFT实现数字脉冲压缩的原理框图

**出题说明（评审要点）**：形态未变，沿用 v0 `vis_000201`。**D-P2-3 要的"理解架构图"就是这个形态**： 问的不是数值，而是**结构**（有哪些环节、先后顺序）。真值来自图上的**可见标注文字**， 所以判分用要点覆盖是合适的；⚠️ 仍须人工核对（`verified_by: manual-pending`）。 预处理：原图 859×251 → 补白到 864×256（保几何、不 resize）。

### 22. `vis_000302`　读图取值（readout）

`subdomain=ambiguity`　`regime=universal`　`difficulty=medium`　`modality=vision`　`ask_style=explicit`

`tokens=384`（文本 145 + 图像 239，图 1 张）

**图（512×480，sha256 `1e288884163f62e0…`）**

![vis_000302](../data_processed/figs_vision/1e288884163f62e0813cf6381c466150e8b5aef91f2e205c1dacc187279a32a5.jpg)

`figure_truth`：`{"kind": "textbook_figure", "pdf_page": 84, "caption": "(u) 矩形脉冲距离模糊函数", "observed": {"tau_zero_us": 2.0, "peak_value": 1.0, "shape": "三角形"}, "verified_by": "manual-pending"}`

**问（user）**

> 图中横轴为延迟时间 τ、纵轴为模糊函数的归一化幅值。请读出曲线降为零的 τ 位置，并据此说明该矩形脉冲的宽度 t_p。

**答（assistant，模型实际会看到的内容）**

```text
曲线在 τ = ±2 μs 处降为零，因此该矩形脉冲的宽度 t_p = 2 μs。
```

**判分**：`{"type": "keyword", "value": "曲线在 τ = ±2 μs 处降为零，因此该矩形脉冲的宽度 t_p = 2 μs。", "keypoints": ["±2 μs", "矩形脉冲的宽度"]}`

**出处**：《机载雷达系统与信息处理》第2章 2.4.1（PDF p84，图2.4.4(u)）
> 单个矩形脉冲的距离模糊函数在 |τ| ≤ t_p 内为 1 − |τ|/t_p，在 |τ| > t_p 时为零。

**出题说明（评审要点）**：形态未变，沿用 v0 `vis_000202`。**readout 的取法**：只问图上**能读出来的量** （零点的横轴位置 ±2 μs），不问图上没有的信息。由 |χ(τ,0)|=1−|τ|/t_p 在 |τ|=t_p 处归零， 可反推 t_p = 2 μs —— 这一步是**可验证的推理**，而不是"看图猜"。 ⚠️ 真值须人工核对横轴刻度。

### 23. `vis_000303`　读图趋势（trend）

`subdomain=ambiguity`　`regime=universal`　`difficulty=hard`　`modality=vision`　`ask_style=explicit`

`tokens=481`（文本 142 + 图像 339，图 1 张）

**图（640×544，sha256 `7a1d33522a05c675…`）**

![vis_000303](../data_processed/figs_vision/7a1d33522a05c6757433db408964bd846cba56154e8c92c4e1c18091be8b7e1f.jpg)

`figure_truth`：`{"kind": "textbook_figure", "pdf_page": 91, "caption": "图2.4.14 线性调频信号的模糊函数图", "observed": {"ridge": "斜向", "peak_value": 1.0, "axes": ["多普勒频率/MHz", "延迟时间/μs"]}, "verified_by": "manual-pending"}`

**问（user）**

> 图中是线性调频信号的模糊函数三维图。请描述其主峰脊线在延迟-多普勒平面上的走向，并说明这说明什么。

**答（assistant，模型实际会看到的内容）**

```text
峰脊沿斜线方向倾斜延伸而不是与坐标轴平行，说明延迟与多普勒之间存在耦合，这是线性调频信号的典型特征。
```

**判分**：`{"type": "keyword", "value": "峰脊沿斜线方向倾斜延伸而不是与坐标轴平行，说明延迟与多普勒之间存在耦合，这是线性调频信号的典型特征。", "keypoints": ["斜线", "耦合", "线性调频"]}`

**出处**：《机载雷达系统与信息处理》第2章 2.4.3（PDF p91，图2.4.14）
> 图2.4.14 线性调频信号的模糊函数图

**出题说明（评审要点）**：形态未变，沿用 v0 `vis_000203`。**trend 考"读走向"而不是读点**： 矩形脉冲是正三角形（沿 τ 轴），线性调频则把主峰"斜着拉长" —— 这一斜就是 **距离-多普勒耦合**的图形化表达，也是 LFM 能用"多普勒失配换时延"的原因。 ⚠️ 描述性真值最容易过度解读：要点只落在图上确实可见的（斜脊）与教材明确成立的（耦合）。

### 24. `vis_000304`　图内对比（compare）

`subdomain=misc`　`regime=airborne_pulse`　`difficulty=hard`　`modality=vision`　`ask_style=explicit`

`tokens=393`（文本 170 + 图像 223，图 1 张）

**图（512×448，sha256 `a738c00a4f7c12d4…`）**

![vis_000304](../data_processed/figs_vision/a738c00a4f7c12d426fdab106ca1b4e72aa5cadd167357ae39f33ba4c07395b0.jpg)

`figure_truth`：`{"kind": "textbook_figure", "pdf_page": 289, "caption": "图 7.6.7 最优及加权全自适应 STAP 的 SINR", "observed": {"passband_db_min": 20.0, "gap_db_approx": 1.5, "n_curves": 2}, "verified_by": "manual-pending"}`

**问（user）**

> 图中给出机载雷达两种空时自适应处理（STAP）方式的输出 SINR 随目标多普勒频率的变化。请比较两条曲线在通带中部（约 50~250 Hz）的差异。

**答（assistant，模型实际会看到的内容）**

```text
通带中部二者都维持在 20 dB 以上；实线「最优全自适应」比虚线「加权全自适应」高约 1~2 dB，且虚线的起伏更深。
```

**判分**：`{"type": "keyword", "value": "通带中部二者都维持在 20 dB 以上；实线「最优全自适应」比虚线「加权全自适应」高约 1~2 dB，且虚线的起伏更深。", "keypoints": ["20 dB", "1~2 dB", "起伏"]}`

**出处**：《机载雷达系统与信息处理》第7章 7.6.2（PDF p289，图7.6.7）
> 在机载雷达中，地杂波强且存在空间谱和多普勒耦合现象，采用空时自适应信号处理（STAP）可以有效地消除这些地杂波。

**出题说明（评审要点）**：形态未变，沿用 v0 `vis_000204`。**compare 考的才是"代价"**： 加权（降维）自适应是为了把计算量降下来，代价就是 SINR 掉一点、起伏更深 —— 图上能直接看出来。 ⚠️ 数值容差只能给到"约 1~2 dB"这种粒度：图是教材插图，读数精度有限，**不写成精确值**。
