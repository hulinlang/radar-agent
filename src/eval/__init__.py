"""评测判分层（口径 v3）。

⚠️ **单一真源**：判分逻辑只能有一份。
`scripts/p5_eval_report_v3.py`（P5/P5b 对比报告）与 `scripts/p7_eval_e2e.py`（P7 端到端）
都必须从这里 import —— 否则两套判分 → 数字不可比，而"相对基线提升多少"正是验收口径。

迁移动机（2026-09-21）：判分函数原先写在 `p5_eval_report_v3.py` 的**模块级**，
而那个脚本的文件末尾会直接执行主流程（读文件→算分→生成 HTML）。
P7 想复用判分器时 `exec_module` 会连带跑一遍主流程并因找不到命令行参数而崩溃。
→ 判分属**无副作用的可复用逻辑**，按分层铁律本就该在 `src/`。
"""
