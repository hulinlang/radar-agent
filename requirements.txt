# =============================================================================
# radar-agent 运行依赖
#
# 版本下限 = 本项目**实测通过**的版本（见文件末尾注释）。
# ⚠️ torch 不在此列表 —— 必须先按你的 CUDA 版本自行安装，否则会装成 CPU 版。
#    实测环境：torch 2.11.0+cu128（RTX 4060 Laptop，驱动 616.92，WDDM）
#    安装示例（CUDA 12.8）：
#      pip install torch --index-url https://download.pytorch.org/whl/cu128
# =============================================================================

# ---- 模型与微调 ----
transformers>=5.17.0
peft>=0.21.0
accelerate>=1.15.0
safetensors>=0.8.0
tokenizers>=0.23.2
huggingface_hub>=1.31.0

# ---- 检索（BM25 字面匹配 + 中文分词）----
rank_bm25>=0.2.2
jieba>=0.42.1

# ---- 演示服务 ----
fastapi>=0.141.1
uvicorn>=0.53.0
pydantic>=2.13.5

# ---- 基础 ----
numpy>=2.5.3
PyYAML>=6.0.3
tqdm>=4.70.1
requests>=2.34.2

# ---- 可选：视觉题（生成/读取合成雷达图）----
pillow>=12.3.0

# =============================================================================
# 可选：PDF 解析（MinerU）
#   只有"从原始 PDF 重建知识库"时才需要。⚠️ 建议装在**独立虚拟环境**里
#   （MinerU 依赖较重，与训练/推理环境混装易冲突），用法见 README §从零重建。
#   mineru[core]>=2.0
# =============================================================================
